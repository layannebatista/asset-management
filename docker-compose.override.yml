services:
  # Modo completo local (sem testes): remove profiles para subir observabilidade, IA e relatórios
  prometheus:
    profiles: !reset []

  influxdb:
    profiles: !reset []

  grafana:
    profiles: !reset []

  cadvisor:
    profiles: !reset []

  rtk-dashboard:
    profiles: !reset []

  allure:
    profiles: !reset []

  allure-ui:
    profiles: !reset []

  sprint-reporter:
    profiles: !reset []
